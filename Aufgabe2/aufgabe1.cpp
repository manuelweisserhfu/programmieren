#include <stdio.h>
#include <cmath>

int main() {
    int n[7];
    int m[4];
    float f[7];
    char c[5];
    bool b[4];

    n[0] = 17 - 2 * 7 - 9 % 6;
    n[1] = (17 - 2) * (7 + 9) % 6;
    n[2] = (17 - 2) * ((7 + 9) % 6);
    n[3] = ((((17 - 2) * 7) - 9) * 7 + 9) % 6;
    n[4] = 17 - (2 * (7 + (9 % 6)));
    n[5] = 17 / (5 / 3) * 4;
    n[6] = (17 / 5) * (5 / 17);

    f[0] = 17.0 / (5 / 3) * 4;
    f[1] = (17.0 / 5) * (5 / 17);
    f[2] = 1 * (1.0 / 3) * 3;
    f[3] = 1.5e2 * 1.5e2;
    f[4] = 1.5e2 * 1.5e-2;
    f[4] = 1.5e-2 * 1.5e-2;

    c[0] = 'a' + 5;
    c[1] = '0' + 9;
    c[2] = '0' + 9 / 2;
    c[3] = '0' + 9 - 2;
    c[4] = '0' + '9';

    c[0] = '5';
    b[0] = (c[0] >= '0' && c[0] <= '9');
    b[1] = (c[0] >= '0' || c[0] <= '9');
    b[2] = (c[0] >= 0 && c[0] <= 9);
    b[3] = (c[0] >= 0 || c[0] <= 9);

    m[0] = 44 >> 2;
    m[1] = 44 << 1;
    m[2] = 1 << 10;
    m[3] = 1 << 32;

    printf("n: ");
    for (int i = 0; i < 7; i++) {
        printf("%d ", n[i]);
    }
    printf("\n");

    printf("f: ");
    for (int i = 0; i < 7; i++) {
        printf("%.2f ", f[i]);
    }
    printf("\n");

    printf("c: ");
    for (int i = 0; i < 5; i++) {
        printf("%c ", c[i]);
    }
    printf("\n");

    printf("b: ");
    for (int i = 0; i < 4; i++) {
        printf("%d ", b[i]);
    }
    printf("\n");

    printf("m: ");
    for (int i = 0; i < 4; i++) {
        printf("%d ", m[i]);
    }
    printf("\n");

    return 0;
}